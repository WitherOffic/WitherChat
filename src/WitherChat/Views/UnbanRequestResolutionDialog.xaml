<Window x:Class="WitherChat.Views.UnbanRequestResolutionDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Icon="/Assets/WitherChat.ico"
        Width="500" Height="420" ResizeMode="NoResize"
        WindowStartupLocation="CenterOwner"
        Background="{DynamicResource AppBackground}"
        Foreground="{DynamicResource PrimaryText}"
        FontFamily="{DynamicResource AppFontFamily}"
        FontWeight="{DynamicResource AppFontWeight}">
    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="16" />
            <RowDefinition Height="*" />
            <RowDefinition Height="16" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <StackPanel>
            <TextBlock x:Name="TitleText" Style="{StaticResource SectionTitleTextStyle}" />
            <TextBlock x:Name="UserText" Foreground="{DynamicResource MutedText}" Margin="0,5,0,0" />
        </StackPanel>
        <Border Grid.Row="2" Style="{StaticResource GlassCardStyle}" Padding="18">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="12" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="100" />
                </Grid.RowDefinitions>
                <TextBlock Text="{DynamicResource UnbanRequestMessage}" Style="{StaticResource CaptionTextStyle}" />
                <TextBlock Grid.Row="1" x:Name="RequestText" TextWrapping="Wrap" Margin="0,5,0,0" />
                <TextBlock Grid.Row="3" Text="{DynamicResource ResponseToUser}" Style="{StaticResource CaptionTextStyle}" />
                <Grid Grid.Row="4" Margin="0,5,0,0">
                    <TextBox x:Name="ResponseText" AcceptsReturn="True" TextWrapping="Wrap"
                             VerticalContentAlignment="Top" MaxLength="500" Padding="12,10,12,22"
                             TextChanged="ResponseText_TextChanged" />
                    <TextBlock x:Name="ResponsePlaceholder" Text="{DynamicResource ResponseOptional}"
                               Foreground="{DynamicResource MutedText}" IsHitTestVisible="False" Margin="13,10,0,0" />
                    <TextBlock x:Name="ResponseCount" Text="0 / 500" Foreground="{DynamicResource MutedText}"
                               FontSize="11" HorizontalAlignment="Right" VerticalAlignment="Bottom" Margin="0,0,10,6" />
                </Grid>
            </Grid>
        </Border>
        <StackPanel Grid.Row="4" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="{DynamicResource Cancel}" Click="Cancel_Click" Margin="0,0,8,0" />
            <Button x:Name="SubmitButton" Click="Submit_Click" Style="{StaticResource PrimaryCapsuleButtonStyle}" />
        </StackPanel>
    </Grid>
</Window>
